ktor {
    datasource {
        name = "market"
        url = "jdbc:h2:mem:test;init=runscript from 'classpath:schema.sql'"
        username = "market"
        password = "market"
        className = "org.h2.Driver"
    }
}
